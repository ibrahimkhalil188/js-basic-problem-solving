const secntence = "I am a studen of P-hero";
let reverse = "";
for (const element of secntence) {
    reverse = element + reverse;

}
console.log(reverse)

// array have a build in mathod if you need just google it and apply .